import ApproverBoard from '../components/ApproverBoard'

export default function BytesDashboard() {
  return (
    <ApproverBoard
      title="BYTES — Clearance Approvals"
      subtitle="Approve students once the BYTES organization fee and any fines are settled. Verify payments under Payments."
      kind="bytes_officer"
    />
  )
}
