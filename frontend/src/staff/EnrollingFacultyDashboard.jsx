import ApproverBoard from '../components/ApproverBoard'

export default function EnrollingFacultyDashboard() {
  return (
    <ApproverBoard
      title="Enrolling Faculty — Final Clearance"
      subtitle="Final stage of the pipeline. The clearance PDF unlocks for students after this approval."
      kind="enrolling_faculty"
    />
  )
}
